# Shared ingestion code

Reserve this directory for helpers used by multiple examples. The initial Daymet example uses only the Python standard library and is self-contained. Add shared code when a second implementation demonstrates a concrete need; full series transformations stay in their series repository.
